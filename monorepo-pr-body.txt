Updates `packages/intentd` submodule to commit 04d577deae51b59672c78697907947619854075b.

## Submodule PR

Refs: intent-hq/intentd#145

## Changes

- **Direct-mode sandbox provisioning**: CoW-based isolated agent sandboxes
- **APFS CoW probe fix**: Fixed false-negative bug in f_fsid fast-path that silently disabled CoW on APFS
- Sandbox lifecycle management (provision/discard/GC)
- Migration 0038: `sandbox` table with FK to `agent_session`
- All sandbox tests execute and pass on APFS

## Testing

All intentd quality gates pass:
- `cargo fmt --check` ✅
- `cargo clippy --workspace -- -D warnings` ✅  
- `cargo test --workspace` ✅ (1,677 tests pass)
