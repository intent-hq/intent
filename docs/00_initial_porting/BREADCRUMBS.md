# Initial Porting — Breadcrumbs

A living progress log for the **initial port of Intent's backend to a headless Rust daemon** (`intentd`). This is the durable trail future agents read first to understand where the port stands, and append to as work lands.

See also: [IMPLEMENTATION_SPEC.md](./IMPLEMENTATION_SPEC.md) (target architecture), [PROTOCOL.md](./PROTOCOL.md) (wire contract), and the root [AGENTS.md](../../AGENTS.md) (workflow + breadcrumb-update policy).

## Goal

Port Intent's backend to a standalone, headless Rust daemon (`intentd`) speaking **JSON-RPC 2.0 over a Unix-domain socket**, local-first. Scope of **this** effort is the **Rust backend only** — no frontend (Tauri/Svelte) yet. The Electron app in `augmentcode/intent` is the behavioral ancestor; the wire contract is defined in PROTOCOL.md.

## Current submodule HEAD

- `packages/intentd` @ `0f1f2f8` (**Milestone 9 — Agent ecosystem**: five landed surfaces — **(1)** a **settings store** (the additive `0008_settings.sql` store + `settings_repo`) behind `settings.*`: typed get/list + an **atomic batch `update`** with `-32602` validation, **keychain-backed redaction** of sensitive keys (`workspace.sshKeyPath`, `mcp.servers`, `server.auth.token`, `sourceControl.github.token`), the self-sufficient `settings:changed` event, and the previously-deferred **`git.autoCommit` gate** now wired onto this store; **(2)** `rules.*` over the `endUserRules` setting plus the internal **prompt-assembly/injection** pipeline (base → specialization → workspace override → live rule files) wired into agent creation, alongside the per-agent-type **tool denylist**; **(3)** `specialist.*` full CRUD including the previously-missing ported **`specialist.list`** with **3-tier resolution** (project > user > bundled; bundled read-only; markdown-with-frontmatter storage); **(4)** `mcp.servers.*` (7: list/create/update/delete/toggle/restart/getStatus) over the sensitive `mcp.servers` setting (env/headers redacted) backed by an **`McpHub`** that spawns/stops/restarts external **stdio** MCP servers (handshake → toolCount) with a health monitor + auto-restart, the self-sufficient **`mcp.servers:status-changed`** event per transition, and process-group reaping on shutdown (http/sse transports deferred via an error status; the §6.8 agent→BE callback is the opposite direction and untouched); and **(5)** the `memories` table (the additive `0009_memories.sql` migration + `idx_memories_ws` + `memories_repo`) backing **`search.memories`** — promoted from the M4 empty stub to a real store-backed substring search returning `MemoryMatch[]` per PROTOCOL §5.15 (**no `memories.*` RPC** — deferred §18.5). 583 tests green; landed via intentd PR #28. Atop the prior **Milestone 8 — deferred `accept-changes.execute` git actions now real**: the five previously-deferred `accept-changes.execute` actions — **`undo-commit`, `undo-push`, `reset-to-trunk`, `rebase-onto-trunk`, and `merge`** — are now implemented end-to-end via **libgit2 primitives (no shelling out to `git`)** in the `intent-git` crate (`auth`/`fetch`/`reset`/`refs`/`ls_remote`/`squash`/`stash`/`rebase_with_autostash` + `push_refspec`), wired through `intent-services` → `intent-transport` and **wire-for-wire parity-checked against the TS `accept-changes.service.ts`**; **`export` remains deferred** for the documented `exportPath` / path-safety reason. 569 tests green. Landed via intentd PR #27. Atop the prior **Milestone 7 COMPLETE — stable client identity (`client.hello`) + BE-persisted per-client chat drafts (`drafts.*`) + the `draft:changed` event**: a logical, client-supplied `clientId` that survives reconnects — minted server-side when omitted, idempotent re-hello (upsert), and a server **capability block** in the response — plus the `drafts.*` namespace (`get`/`set`/`clear`) keyed by `(workspaceId, agentId, clientId)` with **empty-`set`==clear** and strict per-client isolation, and the self-sufficient **`draft:changed`** event (`{ workspaceId, agentId, clientId, hasDraft }`) that signals a draft exists/was cleared **without leaking text**. Backed by the additive **`0007_client_drafts.sql`** migration (the `client` + `draft` tables; `0001`–`0006` untouched). `client.hello`/`drafts.*` are **intentd transport extensions** resolving the effective `clientId` **connection-side** (never a wire parameter). Landed via intentd PR #26. Atop the prior **Milestone 6 (Iteration 3) COMPLETE — `terminal.*` + `script.*` on the unified PTY host + the workspace activity/attention status model + locality/host capabilities + remote-only mitigations (T1–T6 landed)**: **(a)** the `intent-pty` crate — a unified `portable-pty` **host** (`PtyHost`) in which interactive terminals **and** scripts run as real PTYs in one host (so a terminal can attach to a running script), with bounded server-side scrollback (back-fill recent history then tail live), multi-client output fan-out, serialized stdin into the single PTY master, resize + SIGINT signal delivery, and session/workspace-scoped process-group reaping (the PTY child is a `setsid` leader so `killpg` reaps the whole tree — no orphans); engine-only, it exposes a clean API for the wire layers — now consumed by the **`terminal.*`** RPC + ACP `terminal/*` surface (T2) and the **`script.*`** surface (T3), both reconciled onto this single host. **(b)** the **workspace activity/attention status model** (§9.9) — a derived `WorkspaceActivity` (`Idle`/`AgentRunning`, **never persisted**, computed from live agent state), server-owned persisted `attention` (`None`/`Unread`/`ReviewRequired`, reusing the existing column + the existing `workspace.markSeen`/`dismissAttention` RPCs), and self-sufficient **only-on-change** `workspace:activity-changed`/`attention-changed` events. Atop the **Milestone 5 — Cycle G COMPLETE** base: the §11.3 security review + hardening, the hermetic mock ACP harness (§13.2), the E2E transport + lifecycle suite (§13.1), and the Cycle F daemon (mDNS discovery, single-instance enforcement + idle reaping, CLI control + daemonization)). **(c)** **locality detection & host capabilities**: a `host.status` probe (`os`/`arch`/`hostname`/`hasDisplay`/`locality`/`displayServer?`) answered on **both** UDS and TCP/WSS, backed by a pure `resolve_is_local()` (UDS / `--mode local` ⇒ local, TCP/WSS ⇒ remote, with a `server.locality` setting override winning), and `hasDisplay`/`locality` folded into the `status`/`doctor` reports and the mDNS TXT record. **(d)** **remote-only mitigations**: `forward.*` (create/list/close) real per-connection **loopback TCP tunnels** (no-op when local) with a registry + connection-drop cleanup, an FE-served **`host.openExternal`** as a daemon→client **reverse RPC** (a `ReverseChannel` mirroring the ACP client-served pattern — resolves locally via an injectable opener when local+`hasDisplay`, proxies to the connected client when remote), and a clear **headless warning** when local+`hasDisplay`=false)

## Implemented surface so far

- **JSON-RPC methods (142 request methods + 1 server-initiated notification):** full CRUD across four domains plus the event, local-git, PR, code-review, search, interactive-terminal, script, settings, rules, specialist, and MCP-server surface over UDS, backed by SQLite —
  - `workspace.*` (9): `list`, `get`, `create`, `update`, `archive`, `unarchive`, `delete`, `markSeen`, `dismissAttention`.
  - `note.*` (12): `list`, `get`, `create`, `update`, `setContent`, `add`, `edit`, `editLines`, `updateMetadata`, `listTasks`, `readAsset`, `delete`.
  - `task.*` (8): `markAsTask`, `update`, `updateStatus`, `updateNoteStatus`, `getMyTask`, `createPrerequisite`, `assignAgent`, `convertBlocks`.
  - `comment.*` (5): `add`, `list`, `getThread`, `respond`, `delete`.
  - `event.*` (7): `recentFiles`, `agentActivity`, `workspaceSummary`, `directoryChanges`, `query` (query/aggregation), plus the deprecated singular `subscribe`/`unsubscribe` aliases.
  - `events.*` (2 + notification): the `events.subscribe`/`events.unsubscribe` subscription fast-path and the server-initiated `events.event` push notification.
  - `git.*` (6): `status`, `stage`, `getBranches`, `commit`, `agentCommit`, `checkMergeConflicts`.
  - `pr.*` (12): 5 reads + 7 writes/actions, all **active-PR gated** (`-32603` when no active PR is linked).
  - `file-tracking.*` (8): `init`, `sync`, `load`, `getChanges`, `loadCommits`, `getLineStats`, `stage`, `unstage`.
  - `metrics.*` (4): `getWorkspaceStats`, `getAgentStats`, `getAllWorkspaceStats`, `clearAgentStats`.
  - `accept-changes.*` (5): `getStatus`, `prepare`, `execute`, `mergePR`, `addRemote`.
  - `search.*` (8): `inFiles`, `fileNames`, `messages`, `events`, `memories`, `notes`, `codebase`, `cancel` — `search.memories` is now a **real store-backed substring search** over the `memories` table (per §5.15); the **`memories.*` RPC** is **NOT PORTING** (vestigial stub; §18.5), and `search.codebase` is **ripgrep/symbol-backed** with auggie codebase-retrieval **not porting** (Milestone 10, Option A — auggie exposes no retrieval CLI).
  - `terminal.*` (6): `create`, `write`, `resize`, `kill`, `getBuffer`, `list`.
  - `script.*` (9): `list`, `create`, `remove`, `start`, `stop`, `restart`, `run`, `output`, `status`.
  - `settings.*` (4): `list`, `get`, `update`, `reset` — typed get/list + an atomic batch `update` (`-32602` validation), with **keychain-backed redaction** of sensitive keys (`workspace.sshKeyPath`, `mcp.servers`, `server.auth.token`, `sourceControl.github.token`); emits `settings:changed`. The previously-deferred `git.autoCommit` gate now reads this store.
  - `rules.*` (3): `list`, `get`, `update` — over the `endUserRules` setting, feeding the prompt-assembly/injection pipeline (base → specialization → workspace override → live rule files) wired into agent creation, plus the per-agent-type tool denylist.
  - `specialist.*` (5): `list`, `get`, `create`, `edit`, `delete` — full CRUD incl. the previously-missing ported `specialist.list` with 3-tier resolution (project > user > bundled; bundled read-only; markdown-with-frontmatter storage).
  - `mcp.servers.*` (7): `list`, `create`, `update`, `delete`, `toggle`, `restart`, `getStatus` — over the sensitive `mcp.servers` setting (env/headers redacted); an `McpHub` spawns/stops/restarts external **stdio** MCP servers (handshake → toolCount) with a health monitor + auto-restart, emits `mcp.servers:status-changed` per transition, and reaps process groups on shutdown (**stdio only — http/sse transports are NOT PORTING**, returning an error status; Milestone 10).
  - `host.*` (1, intentd transport extension): `status` — a locality/capability probe (`os`/`arch`/`hostname`/`hasDisplay`/`locality`/`displayServer?`) answered on both UDS and TCP/WSS. Plus the FE-served **`host.openExternal`** as a daemon→client **reverse RPC** (not a client request method).
  - `forward.*` (3, intentd transport extension): `create`, `list`, `close` — per-connection loopback TCP tunnels (no-op when local).
  - `client.hello` (1, intentd transport extension) + `drafts.*` (3, intentd transport extension): `client.hello` upserts a logical, client-supplied `clientId` (the `client` row — name + capability bag), **minting one server-side when omitted**, idempotent on re-hello, and returns a server **capability block**; `drafts.get`/`drafts.set`/`drafts.clear` persist per-client chat drafts keyed by `(workspaceId, agentId, clientId)` where the `clientId` is resolved **connection-side** from `client.hello` (never a wire parameter), an empty `drafts.set` text is a **clear** (row deleted), and clients are **isolated** from one another. Backed by the additive `0007_client_drafts.sql` migration (the `client` + `draft` tables).
- **Events:** append-only `Event` domain + event-type taxonomy (`intent-core`); durable append-only event log via the `0003_events` migration + `event_repo` (`intent-store`); in-process `EventBus` + filter engine with `subscriber_count` observability (`intent-services`); bidirectional UDS transport that pushes `events.event` and serves the `events.subscribe`/`unsubscribe` fast-path (`intent-transport`); a notify-based file watcher emitting debounced `file:changed` events (`intent-services`); and the M1 workspace/note/task/comment CRUD mutations now emit change events onto the bus. Plus the **`draft:changed`** event emitted by `drafts.set`/`drafts.clear` — a self-sufficient `{ workspaceId, agentId, clientId, hasDraft }` payload that deliberately **omits the draft text** (no leakage), signalling only that a client's draft exists or was cleared. Plus two M9 events: **`settings:changed`** (emitted by `settings.update`) and the self-sufficient **`mcp.servers:status-changed`** (emitted per MCP-server lifecycle transition).
- **CLI:** `intentd serve`, `intentd call`, `intentd status` (renders the live `system.status` control RPC — liveness, transports, port, clients, agents, cert fingerprint, host caps — with a clean down fallback), `intentd stop` (graceful `system.shutdown` RPC then SIGTERM→SIGKILL escalation via pidfile, behind a testable `Signaller` seam), `intentd doctor` (verifies migrations are applied incl. the `0004` agent_session gate + reports provider discovery — which providers are installed/authed — plus ports-free / cert-validity / `gh`+`GITHUB_TOKEN` / context-engine / `hasDisplay`+locality checks), `intentd service install|uninstall|status` (generates a launchd plist + systemd user unit, platform-gated, no double-fork), and `intentd mcp-bridge --connect <addr>` (the stdio↔TCP MCP proxy referenced from a generated `--mcp-config`).
- **Transport:** JSON-RPC 2.0 router over UDS (newline-delimited, mode `0600`, stale-socket cleanup, SIGINT/SIGTERM handling), error codes `-32700`/`-32600`/`-32601`/`-32602`/`-32603`, plus server-initiated `events.event` notifications. UDS listener + control client are `cfg`-gated so non-Unix targets (Windows) build cleanly. A UDS-only **control fast-path** (`transport::control`, the `SystemControl` trait) handles `system.status`/`system.shutdown`, intercepted before the JSON-RPC dispatcher (`serve_uds` takes an optional control handler); these are intentd-only ops methods, **not** part of the ported 106. **WSS transport (Milestone 5 — Cycle E):** an HTTPS + WS listener on `0.0.0.0:5180` exposing `/ws` + `/health`, wire-identical to UDS via a shared router/connection layer (`transport::conn`). `transport::tls` provides rcgen self-signed EC P-256 cert generation + SHA-256 fingerprint pinning (`ring` as the sole rustls provider); `transport::auth` provides a keychain-backed bearer token (timing-safe validate, extracted from the `Authorization` header or `?token=`) + an origin allow-list; `transport::ws` + `transport::lifecycle` handle upgrade-time auth (`401`/`403`), port backoff, single-flight start/stop, graceful shutdown (no `EADDRINUSE`), a 30s/60s heartbeat, and a post-bind handler.
- **Persistence:** SQLite via `sqlx` with embedded migrations (WAL, `foreign_keys`, `busy_timeout`), incl. the `0002_comments` and `0003_events` migrations.
- **Tests:** end-to-end UDS lifecycle + events integration tests plus camelCase wire-parity fixtures.
- **ACP foundation:** provider registry + model resolution (`intent-providers`); ACP client core — subprocess spawn, hand-rolled NDJSON JSON-RPC transport, reader/dispatch loop, `initialize` handshake, session lifecycle (`new`/`load`/`prompt`/`cancel`) with `session/update`→event-bus streaming, client-served filesystem read/write, permission-request flow, and a terminal stub (`intent-acp`); the `AgentSession` entity + `agent_repo` persistence via the `0004_agent_session` migration; and the `WorkspaceApi` trait (`intent-core`).
- **ACP orchestration (Cycle B):** the `AgentManager` multiplexer + process registry (global concurrency cap + LRU idle eviction) wired into `cmd_serve` (`intent-services`); the agent→BE **MCP callback server** (`WorkspaceMcpServer`) over the same `Arc<dyn WorkspaceApi>` the FE uses, universal MCP-config translators (`toAcpMcpServers`/`toAuggie…`/`toCodex…`/`toOpenCode…`), baseline-env injection + secret redaction, and the per-agent-type tool denylist (§18.4) (`intent-acp`); the **`agent.*` RPC surface** (24 methods); and the **live MCP spawn-wiring** — `AgentManager::create_agent` stands up a per-agent denylisted MCP server, serves it over a loopback **bridge** (`intent-acp::mcp_bridge`: a TCP NDJSON listener + the `intentd mcp-bridge` stdio↔TCP proxy), and writes the generated `--mcp-config` so a real spawned child reaches the in-process server. Proven by a hermetic spawned-child **E2E** (the `mock` provider's node agent performs a real `add_to_note` MCP `tools/call`; asserts BE state change + streamed chunks + exactly one `agent:stream:end` + persisted conversation).
- **Agent runtime wiring (M3.10):** the `agent.*` RPC handlers are now wired to the `AgentManager` **live runtime** rather than stubs (`intent-services`) — `agent.sendMessage` lazily spawns the child on the first turn (the TS spawn-on-first-message semantics), drives the ACP turn through `AgentManager::run_turn` in a background worker, and drains the in-memory send queue (flipping each queued message to in-flight when the current turn ends, the **queue-flip** loop); `agent.forceMessage` interrupts (aborts) the in-flight worker then delivers immediately; `agent.stop` aborts the in-flight turn worker, clears the busy flag, and drops the handle (killing the child via `kill_on_drop`) + deregisters. Proven over the wire by the new **over-the-wire E2E** `tests/uds_agent_runtime.rs`: it launches the real `intentd serve` daemon (mock provider) and drives `agent.create` + `agent.sendMessage` over UDS, asserting the daemon-spawned child reached the per-agent workspace MCP server via the generated `--mcp-config` (mutating the note) and that both the tool's `note:updated` domain event and the terminal `agent:stream:end` arrive over the transport.
- **GitHub / PR (Milestone 4 — Cycle B):** a new `intent-sourcecontrol` crate — the `SourceControl` trait + `GitHubSourceControl` over **octocrab** (REST + GraphQL), token resolution (keyring → env → `gh` CLI), GitHub Enterprise (GHE) base-URL support, a provider **registry**, and graceful `NotConfigured` behavior when no token/host is available. Backs the full **12-method `pr.*` surface** (5 reads + 7 writes/actions, active-PR gated with `-32603`) routed end-to-end (`intent-sourcecontrol` → `intent-services` `pr_ops` → `intent-transport`). Plus **PR↔workspace linking**: `head.ref` **branch-only** matching, the `0005_workspace_pr` migration persisting `prStatus` + `activePullRequest`, and a 60s background + on-demand refresh emitting `pr:linked`/`pr:updated`/`pr:unlinked` events. Touched `intent-core`, `intent-sourcecontrol`, `intent-services`, `intent-store`, `intent-transport`, `intentd`.
- **Local git (Milestone 4 — Cycle A):** a new `intent-git` crate (git2/libgit2, depends on `intent-core` only per §3.2) backing the six `git.*` JSON-RPC methods end-to-end (`intent-git` → `intent-services` `git_ops` → `intent-transport` router): `git.status` (file-level `GitStatus` with branch/ahead/behind/diverged + `files[]`), `git.stage` (CSV-string or array paths; staging all — `.`/`*`/`--all` — rejected with `-32603`), `git.getBranches` (`repoPath` + `includeRemote?`; unknown/unauthorized path → `-32602`, known-repo set derived from persisted workspaces), `git.commit` (deprecated; empty-commit guarded), `git.agentCommit` (`{ ok, hash, files, fileCount }`), and `git.checkMergeConflicts` (libgit2 in-memory `merge_base`+`merge_trees`; no base → `cannotDetermine`). Plus two internal helpers for Cycle C (no wire method yet): a lazy diff helper (additions/deletions + hunks via blob SHAs) and a worktree-create + per-path async lock (`WorktreeLocks`). Parity-exact result shapes/casing vs the TS `ws-git-api` ground truth; proven by a UDS write-ops round-trip test.
- **Code Changes Review (Milestone 4 — Cycle C):** the BE-internal change-tracking + attribution pipeline, metrics aggregation, and accept-changes orchestration — backed by the additive `0006_code_review` migration (the `tracked_changes` per-file audit trail with raw content kept lazy via git blob SHAs, never inlined) and the new `intent-store` repos (`tracked_changes_repo`, `diffs_repo`, `metrics_repo`). Adds **17 JSON-RPC methods** routed end-to-end (`intent-services` → `intent-transport`): `file-tracking.*` (8), `metrics.*` (4), and `accept-changes.*` (5). `accept-changes.execute` implements `commit` (cascading → `push` → `create-pr`), `push`, and `create-pr`, with `accept-changes.mergePR` performing the PR merge; the `ChangeStage` gains a `pull_request` stage with `pushed`→`pull_request`→`merged` transitions. The `intent-git` `WorktreeLocks` mutex is now wired on the services commit path (per-worktree `with_lock`). Emits the new `changes:metrics-changed` event (alongside `changes:tracked`/`changes:git-status`) after a mutating step. Touched `intent-core`, `intent-git`, `intent-services`, `intent-store`, `intent-transport`, `intentd`.
- **Search (Milestone 4 — Cycle D):** a new `intent-search` crate — gitignore-aware content + filename search built on `ripgrep`/`ignore`/`globset` (honors `.gitignore`, a deliberate divergence from the TS behavior), with a `CancelRegistry` for in-flight cancellation by request id. Backs the full **8-method `search.*` surface** routed end-to-end (`intent-search` → `intent-services` `search_ops` → `intent-transport`): `search.inFiles` (content) + `search.fileNames` (filename/path) over the filesystem, the store-backed `search.messages`/`search.events`/`search.notes`, plus `search.memories` and `search.codebase` (surface present; backends deferred) and `search.cancel`. Each search mints + echoes a `requestId`, tracked via the `CancelRegistry`, and streams incremental `search:result` events plus a terminal `search:done` over the event bus. Touched `intent-core`, `intent-search`, `intent-services`, `intent-transport`, `intentd`.
- **Discovery + daemon lifecycle (Milestone 5 — Cycle F):** **mDNS service discovery** (`transport::discovery`, `intent-transport`) advertising the daemon as `_intent-ws._tcp` with `version`/`path`/`hostname`/`fp` plus `os`/`arch`/`hasDisplay`/`locality` TXT records, config-gated, advertised post-bind and unpublished on shutdown. Plus **single-instance enforcement + idle agent reaping**: a pidfile guard with a UDS liveness probe + stale pidfile/socket cleanup so a second daemon refuses to start only when the first is actually serving (`intentd`); a TTL idle-reap sweep extending the LRU agent registry, gated by the new `idleReapMinutes` config knob (default 30) (`intent-services`); and process-group tree-kill (SIGTERM→SIGKILL via `nix`) backed by `process_group(0)` at spawn so a reaped/torn-down agent's whole child tree is removed, not just the direct child (`intent-acp`/`intent-services`).
- **CLI control + daemonization (Milestone 5 — Cycle F / M5.6):** the operator-facing daemon-control surface. A UDS-only **control fast-path** (`transport::control`, the `SystemControl` trait — `intent-transport`) handling `system.status`/`system.shutdown`, intercepted before the JSON-RPC dispatcher (`serve_uds` gains an optional control handler; `detect_has_display` + `inspect_cert` exposed) — these `system.*` methods are **intentd-only** ops/control methods, **not** part of the ported 106 / `getSupportedMethods`. `intentd status` renders live `system.status` (liveness, transports, port, clients, agents, cert fingerprint, host caps) with a clean down fallback; `intentd stop` issues a graceful `system.shutdown` then escalates SIGTERM→SIGKILL via the pidfile (testable `Signaller` seam); `intentd doctor` adds ports-free / cert-validity / `gh`+`GITHUB_TOKEN` / context-engine / `hasDisplay`+locality checks; and `intentd service install|uninstall|status` generates a launchd plist + systemd user unit (golden-string tests, platform-gated, no double-fork). Touched `intent-transport`, `intentd`.
- **Data migration + retention (Milestone 5 — Cycle G):** `intentd import --from <legacy-store>` — an **idempotent, read-only** §9.7 data migration that opens a legacy store and replays it through the existing `intent-store` repos (no new wire methods, no schema changes) (`intentd`). Plus a §10.2 **event retention/compaction sweep**: `Store::delete_stream_events_before(cutoff)` — the sole delete path on the otherwise append-only event log, scoped to the `agent:stream:` prefix with a strict `< cutoff` (`intent-store`); `events::AGENT_STREAM_PREFIX` + `Config.stream_retention_hours` parsed from `events.streamRetentionHours` (camel/snake; env `INTENTD_STREAM_RETENTION_HOURS`; default `0` = disabled) (`intent-core`); and `spawn_stream_retention_loop` wired into the periodic-task loop beside the idle-reap sweep, disabled at TTL=0, aborted on shutdown (`intentd`). The sweep trims only old `agent:stream:*` chunks and **preserves all other event families** (lifecycle/tool/permission/pr/task/note/file/workspace) regardless of age; idempotent re-run; no VACUUM (WAL checkpointing reclaims space passively). Touched `intent-core`, `intent-store`, `intentd`.
- **ACP integration harness + E2E transport/lifecycle suite (Milestone 5 — Cycle G):** a **hermetic mock ACP provider** (§13.2) — an in-process, fixture-driven agent in `intent-acp` that replays a scripted NDJSON frame sequence (no node, no network, no real provider binary) over the real `Connection` + handshake + session lifecycle + client-served `ClientRequestHandler`, covering happy-path turn, auth-required, mid-turn cancel, permission deny/allow, oversized-stderr ring-buffer truncation + terminal stub (`-32601`), malformed-frame recovery, and stdin write-serialization (fixtures under `tests/fixtures/*.ndjson`, directory-override `MOCK_AGENT_SCRIPT_PATH`). Plus the **E2E transport + lifecycle suite** (§13.1, `intentd/tests/e2e_transport.rs`) booting a **real** `intentd serve` and driving it over **both** transports: `e2e_transport_full` (`--listen both`) asserts live `system.status` transports `["uds","tcp"]`, WSS bearer-auth `401`s + accept, origin allow-list `403`/`101`, the pinned TLS fingerprint matching the on-disk cert + the mDNS-advertised `fp`, an mDNS advertise→resolve round-trip, byte-identical WSS vs UDS JSON-RPC results, `intentd status`/`doctor` against the live daemon, and a graceful `intentd stop` + same-port restart with no `EADDRINUSE`; `e2e_idle_session_reaping` (`--listen uds`) drives one mock-ACP turn and asserts the idle-TTL sweep evicts the agent. Each daemon gets a private data dir + OS-assigned free port (hermetic; env-gated test-only seams `INTENTD_AUTH_TOKEN`/`INTENTD_TCP_PORT`/`INTENTD_DISCOVERY`/`INTENTD_IDLE_REAP_MS` fall back to production defaults). Also a deterministic fix for the pre-existing `tests::pr` connection-pool-timeout flake — the tokio clock is now paused only **after** the first SQLite read provably completes (a `Notify` barrier on the stub forge's first `get_pr`), so the pool `acquire_timeout` timer can no longer fire spuriously; no assertions weakened. Touched `intent-acp`, `intent-services`, `intentd`.
- **Security review + hardening (Milestone 5 — Cycle G, §11.3):** a focused audit of the §11.3 requirements confirmed **all 8 items are already implemented in production with no gaps** — UDS socket created `0600`, timing-safe bearer-token comparison, origin allow-list enforcement, TLS fingerprint-pinning / TOFU, path sandboxing, process-group reaping, headless default-deny posture, and secret hygiene (no secrets logged/leaked). The work is **test-only** regression coverage that locks in those guarantees: a new `intentd/tests/security_hardening.rs` suite plus extended `intent-acp` tests (+178/-4). Touched `intent-acp`, `intentd` (tests only).
- **PTY host (Milestone 6 (Iteration 3) — T1, §12.1):** a new `intent-pty` crate (depends on `intent-core` only per §3.2) — a unified `portable-pty` **host** (`PtyHost`) in which interactive terminals **and** scripts run as real PTYs in one host, so a terminal can attach to a running script. Provides PTY spawn/resize/signal (SIGINT) over `portable-pty`; **bounded server-side scrollback** (`scrollback::Scrollback`, porting `script-output-buffer.ts`) so output survives client disconnects and a late-attaching subscriber **back-fills recent history then tails live**; **multi-client output fan-out** (broadcast, porting `MainProcessTerminalManager.ts`) with stdin from any client **serialized** into the single PTY master; and **session/workspace-scoped lifetime** — each PTY carries a scope key, `kill_scope` tears a scope down, and the PTY child is a `setsid` session leader so `killpg` reaps the whole tree (no orphaned grandchildren, reusing the M5 process-group pattern). Engine-only: exposes `PtyHost`/`SpawnSpec`/`Attachment`/`PtyId`/`PtySize`/`PtySignal` for the wire layers (`terminal.*` and `script.*`, deferred to later in Milestone 6) and contains **no** wire methods, script policy, or status model. Touched `intent-pty`.
- **`terminal.*` RPC + ACP `terminal/*` (Milestone 6 (Iteration 3) — T2, §5.13/§6.7/§12):** the interactive-terminal wire surface over the `intent-pty` host. **Two front doors share one `PtyHost`** (owned by `Services`, exposed via `Services::pty()`): **(a)** six additive `terminal.*` JSON-RPC methods routed end-to-end (`intent-core` `WorkspaceApi` trait → `intent-services` `terminal_ops` → `intent-transport` router) — `terminal.create` (spawns a workspace-scoped PTY, default `$SHELL`, returns `{ terminalId }`), `terminal.write` (base64 stdin), `terminal.resize`, `terminal.kill`, `terminal.getBuffer` (base64 scrollback snapshot, optional trailing `maxBytes`), and `terminal.list`. On create, a streamer attaches to the host and fans output onto the event bus as `terminal:data` (base64 `chunk`) + a terminal `terminal:exit` (self-sufficient `{ terminalId, exitCode, signal }`); the streamer polls the child so a **natural exit** (no `terminal.kill`) still emits `terminal:exit`. Late attach **back-fills** via `terminal.getBuffer` then tails live `terminal:data`; multi-subscriber fan-out is inherent to the bus. **(b)** the ACP client-served `terminal/*` (create/output/wait_for_exit/release/kill) now run on the **same host**, replacing the M3 stub: `intent-acp` gains a PTY-free `TerminalHost` trait (mirroring `EventSink`) that `intent-services` implements (`PtyTerminalHost`, scoped to the agent session id) and `AgentManager::create_agent` wires into the `ClientRequestHandler`; the handler keeps the `-32601` stub only as the no-host fallback. New `intent-core` event constants `terminal:data`/`terminal:exit`/`terminal:title`/`terminal:cwd`; new `intent-pty` host accessors `scrollback`/`try_exit`/`wait` + `PtyExit` + `PtyId::parse` (additive). Proven by a real-PTY service round-trip (create→write→getBuffer back-fill→resize→kill, multi-subscriber, natural-exit code), router dispatch tests, and a host-backed ACP `terminal/*` end-to-end (create→wait_for_exit→output→release) extending the mock-ACP suite. camelCase wire parity. Touched `intent-core`, `intent-pty`, `intent-acp`, `intent-services`, `intent-transport`.
- **`script.*` on the unified PTY host (Milestone 6 (Iteration 3) — T3, §5.8/§12):** the full nine-method **`script.*`** wire surface reconciled onto the **same** `intent-pty` host as `terminal.*` — **no separate spawn path**, so a workspace-scoped script PTY is attachable via `terminal.*`. Routed end-to-end (`intent-core` `WorkspaceApi` → `intent-services` `script_ops` → `intent-transport`): `script.list`, `script.create`, `script.remove`, `script.start`, `script.stop`, `script.restart`, `script.run`, `script.output`, `script.status`. **Service-mode** scripts auto-restart with backoff; **command-mode** `script.run` runs to completion with a timeout; dev-server **URL/port detection** surfaces `detectedUrl` on the self-sufficient `script:state` event (alongside start/exit/auto-restart transitions); live output streams as `script:output` (base64 `chunk`); and `script.output` returns **plaintext** last-N-lines buffer text (a bare JSON string, default 100 lines) per §5.8 parity. Touched `intent-core`, `intent-pty`, `intent-services`, `intent-transport`.
- **Workspace activity/attention status model (Milestone 6 (Iteration 3) — T4, §9.9):** `Workspace` now carries a derived `activity` (`WorkspaceActivity::Idle`/`AgentRunning`) computed from live `AgentManager` state and **never persisted** — `workspace.list`/`workspace.get` fill it on read, and `AgentManager` increments/decrements a per-workspace running-agent count on agent start/teardown, emitting a `workspace:activity-changed` only when the derived value flips. The server-owned **`attention`** flag (`None`/`Unread`/`ReviewRequired`) stays **persisted** (reusing the existing column) and is raised/cleared BE-side (`raise_attention`) and via the existing `workspace.markSeen`/`workspace.dismissAttention` RPCs. Both surfaces emit **self-sufficient, only-on-change** events — `workspace:activity-changed { workspaceId, activity }` and `workspace:attention-changed { workspaceId, attention }` (§10.1). Touched `intent-services`, `intent-core`.
- **Locality detection & host capabilities (Milestone 6 (Iteration 3) — T5):** a `host.status` capability probe returning `{ os, arch, hostname, hasDisplay, locality, displayServer? }`, answered on **both** UDS and TCP/WSS, backed by a pure `resolve_is_local()` (UDS / `--mode local` ⇒ local; TCP/WSS ⇒ remote; a `server.locality` setting override always wins). `hasDisplay`/`locality` are folded into the `status`/`doctor` reports and the mDNS TXT record. Implemented in the transport layer (`intent-transport`) + the `intentd` CLI; no new ported domain methods.
- **Remote-only mitigations (Milestone 6 (Iteration 3) — T6):** `forward.*` (`create`/`list`/`close`) real **per-connection loopback TCP tunnels** — **no-op when local** — with a tunnel registry and connection-drop cleanup; an FE-served **`host.openExternal`** implemented as a daemon→client **reverse RPC** (a `ReverseChannel` mirroring the ACP client-served pattern — resolves locally via an injectable opener when local+`hasDisplay`, and proxies to the connected client when remote); and a clear **headless warning** when local+`hasDisplay`=false. Implemented in the transport layer (`intent-transport`) + the `intentd` CLI. With T5+T6, Milestone 6 (Iteration 3) is **complete**.
- **Deferred `accept-changes.execute` git actions (Milestone 8):** the five previously-deferred `accept-changes.execute` actions — `undo-commit`, `undo-push`, `reset-to-trunk`, `rebase-onto-trunk`, and `merge` — are now implemented end-to-end on **libgit2 primitives** (no shelling out to `git`), routed `intent-services` → `intent-transport`. New `intent-git` primitives back them: `auth` (remote credential callback for fetch/push), `fetch`, `reset`, `refs`, `ls_remote`, `squash`, `stash`, `rebase_with_autostash`, and `push_refspec`. Wire-for-wire parity-checked against the TS `accept-changes.service.ts`; **`export` stays deferred** (documented `exportPath` / path-safety reason). Proven by a new `intentd` integration test (`tests/uds_accept_changes.rs`). Touched `intent-git`, `intent-services`, `intentd` (test). Submodule HEAD `be29a53`.

## Deferred / planned (NOT yet built)

- The remaining PROTOCOL methods (106 total in the contract).
- TCP transport. (WSS/TLS transport + bearer-token auth + origin allow-list + TLS fingerprint pinning shipped in Milestone 5 — Cycle E; mDNS discovery + single-instance enforcement + idle reaping + CLI control + daemonization (M5.6) shipped in Milestone 5 — Cycle F.)
- Context engine for `search.codebase` — **RESOLVED (Milestone 10, Option A): won't port auggie retrieval.** Verified against canonical `augmentcode/intent` @ `6d97184` + auggie 0.29.0: the original `codebase:search` was a never-implemented stub returning `[]` with no caller, and auggie exposes **no structured codebase-retrieval CLI**. `search.codebase` is now **ripgrep/symbol-backed** (exceeds the original) and `AuggieContextEngine::retrieve()` returns `Unavailable` instantly; the `ContextEngine` trait + discovery + `doctor` check are retained as forward-looking infra. (The `intent-pty` PTY **host** engine landed in Milestone 6 (Iteration 3) — T1; the `terminal.*` RPC + ACP `terminal/*` **wire** surface landed in T2, and `script.*` reconciled onto the same host in T3 — so the PTY wire surface is complete.)
- **Local git (Milestone 4 — Cycle A) accepted deferrals:** the `autoCommit` gate → **RESOLVED in Milestone 9** (now wired onto the `settings.*` store reading `git.autoCommit`; `assertAgentCommitAllowed` keeps the TS default `true` + `userRequested` bypass); `Agent-Id`/`Linked-Note-Id` commit trailers (no agent context at the UDS layer yet); libgit2 has no pre-commit hooks and no `git merge-tree` (in-memory tree merge used instead); and `getBranches` known-repo set derived from persisted workspaces rather than a repo registry.
- **GitHub / PR (Milestone 4 — Cycle B) accepted deferrals:** PR↔workspace matching is **branch-only** (`head.ref`) vs the TS branch-OR-`baseRef` match; `pr:*` wire events are an **intentd extension** (not in the original TS contract); **capability gating + pagination** deferred (single-page reads); `pr.listComments` returns a **host-agnostic** comment shape; timestamps are **RFC-3339**.
- **Code Changes Review (Milestone 4 — Cycle C):** the `accept-changes.execute` action **`export`** is **NOT PORTING (Milestone 10): won't port** — it has no UI consumer in `augmentcode/intent` and will not exist in the new Svelte/Tauri FE, so `execute` permanently rejects `action:"export"` (cancelled, not deferred). (The other five execute actions — `merge` / `undo-push` / `undo-commit` / `reset-to-trunk` / `rebase-onto-trunk` — **shipped in Milestone 8** via libgit2 primitives; PR-merge is also available via `accept-changes.mergePR`.)
- **Search:** `search.memories` **SHIPPED in Milestone 9** — a real store-backed substring search over the `memories` table; the **`memories.*` RPC** is **NOT PORTING** (Milestone 10 — vestigial in-memory stub with no consumers; cancelled, not deferred — §18.5). `search.codebase` is **ripgrep/symbol-backed** and its context-engine integration is **RESOLVED (Milestone 10, Option A): won't port auggie retrieval** (see the context-engine entry above). gitignore-awareness is a deliberate divergence from the TS behavior.
- **Agent ecosystem (Milestone 9) accepted deferrals:** the T2 specialization layer ports only the `endUserRules` agent-type override (not the reference 3-tier rule-file fallback `.augment/agent-rules/{type}.md` + built-ins); and the §6.8 agent context-source wiring. **`mcp.servers` http/sse transports are NOT PORTING (Milestone 10): won't port** — `intentd` implements **stdio only**; an `http`/`sse` server returns an error status rather than connecting (closes the M9 deferral).
- **Milestone 5 — Cycle E/F/G** are all **shipped (Cycle G COMPLETE → Milestone 5 COMPLETE)**: WSS/TLS transport + bearer auth + origin allow-list + TLS fp-pinning (Cycle E); mDNS discovery + single-instance enforcement + idle reaping + CLI control + daemonization (Cycle F); and data migration (`import`) + event retention + the §13.1 E2E transport/lifecycle suite + the §13.2 mock ACP harness + the §11.3 security review + hardening (Cycle G). Nothing remains deferred in Milestone 5.
- A real **auggie** end-to-end turn in CI: best-effort/local only (requires auggie + login). The hermetic mock-agent E2E is the CI gate; the generated `--mcp-config` + bridge are auggie-consumable.
- `agent.stop` parity follow-ups — **RESOLVED in Milestone 10 (PC-3)**: the keep-alive stop variant via `provider.interrupt` now interrupts and keeps the session warm (rather than killing the child via `kill_on_drop`), and `agent.stop` emits a terminal `agent:stream:end` event.
- Transport panic-safety via `catch_unwind` → `-32603` (currently relies on per-connection `tokio::spawn` isolation).
- Event surface follow-ups — **`task:ready-tasks-changed` emission and the `Event.metadata` field RESOLVED in Milestone 10 (PC-2/PC-1)**; saga-driven `workspace:*` activity/attention events shipped in Milestone 6 (Iteration 3 — T4).
- `file:*` distinct-type modelling — **RESOLVED in Milestone 10 (PC-2)**: distinct `file:*` event types are now emitted (previously the watcher emitted only `file:changed`).
- The entire frontend (Tauri/Svelte).

## Milestone history

### Repo & CI bootstrap

Created two **private** GitHub repos (`cloudlands-ai/monorepo`, `cloudlands-ai/intentd`), a minimal intentd, the monorepo scaffold with `packages/intentd` as a submodule, CI workflows (fmt/clippy/test + 3-target build matrix + semantic-PR-title), and `cliff.toml`.

### Crate skeleton

Scaffolded all 12 crates (per IMPLEMENTATION_SPEC §3) as compiling stubs with §3.2 dependency direction enforced. Verified (`fmt`/`clippy`/`build`).

### Core + SQLite store

`intent-core` (ids, `Error` → JSON-RPC code mapping, `Config`/paths, `Workspace`/`Note` camelCase model, `WorkspaceApi` trait) and `intent-store` (SQLite via `sqlx`, embedded migrations, WAL/`foreign_keys`/`busy_timeout`, repositories). Verified @ submodule `41fd4a6`.

### UDS JSON-RPC slice

`intent-services` (concrete `WorkspaceApi` impl), `intent-transport` (JSON-RPC 2.0 router with the five standard error codes + UDS listener, mode `0600`, newline-delimited, stale-socket cleanup, SIGINT/SIGTERM) and the `intentd` CLI (`serve`/`call`/`status`/`doctor`). Integration test over a temp UDS. Verified @ submodule `2756eb4`.

### READMEs

README.md written for both repos (intentd + monorepo). intentd HEAD `8e13a25`.

### Breadcrumbs, docs index, `make dev`

This milestone: added `docs/00_initial_porting/BREADCRUMBS.md`, `docs/README.md`, the AGENTS.md breadcrumb-update policy, and a `make dev` local dev-stack target.

### Milestone 1 — core domain CRUD

Implemented full create/read/update/delete across the four core domains over UDS: `workspace.*` (9), `note.*` (12), `task.*` (8), `comment.*` (5) — 34 JSON-RPC methods total. Added the `0002_comments` SQLite migration (comment/thread model + repo), end-to-end UDS lifecycle integration test, camelCase wire-parity fixtures, a `doctor` migration-applied check, and `cfg`-gating of the UDS listener + control client so Windows (`x86_64-pc-windows-msvc`) builds cleanly. Spans `intent-core`, `intent-services` (incl. `note_ops`), `intent-store` (incl. `comment_repo`), `intent-transport`, and the `intentd` CLI. CI green on all three build targets (incl. Windows). Submodule HEAD `c989aeb`.

### Milestone 2 — Events

Ported Intent's event system to `intentd`. Added an append-only `Event` domain + event-type taxonomy (`intent-core`) and a durable event log via the `0003_events` migration + `event_repo` (`intent-store`). Built an in-process `EventBus` + filter engine with `subscriber_count` observability (`intent-services`), and extended the UDS transport to be bidirectional — server-initiated `events.event` notifications plus an `events.subscribe`/`events.unsubscribe` subscription fast-path (`intent-transport`). Added the seven `event.*` methods (`recentFiles`, `agentActivity`, `workspaceSummary`, `directoryChanges`, `query`, plus the deprecated singular `subscribe`/`unsubscribe` aliases), a notify-based file watcher emitting debounced `file:changed` events, and wired the M1 CRUD mutations to emit change events onto the bus. Deferred parity-safe: `task:ready-tasks-changed`, saga-driven `workspace:*` events, and `Event.metadata`; distinct `file:*` event types deferred to Milestone 8. CI green on all three build targets (incl. Windows). Submodule HEAD `3238a73`.

### Milestone 3 — Cycle A (ACP foundation)

Landed the **foundation half** of Milestone 3 — the ACP integration's lower layers, ahead of the orchestration half (Cycle B). Added two new crates: `intent-providers` (ACP provider registry — config loading, argument templating, model resolution) and `intent-acp` (ACP client core — subprocess spawn, a hand-rolled NDJSON JSON-RPC transport, a background reader/dispatch loop, the `initialize` handshake; session lifecycle `new`/`load`/`prompt`/`cancel` with `session/update` notifications fanned onto the event bus; client-served filesystem read/write, the permission-request flow, and a terminal stub). Added the `AgentSession` domain entity with store persistence (`agent_repo`) via the `0004_agent_session` migration, and the `WorkspaceApi` trait abstraction (`intent-core`). Accepted deviations: hand-rolled NDJSON transport (acp-tokio 0.11/0.14 incompatible with our dependency set), intentd-new `agent:permission:*` events (sanctioned by PROTOCOL §8), and a `name_explicitly_set` column. Orchestration — AgentManager, MCP callback wiring, the `agent.*` RPC surface, and the end-to-end flow — is the upcoming **Cycle B**. CI green on all three build targets (incl. Windows). Submodule HEAD `f5d2f8f`.

### Milestone 3 — Cycle B (ACP orchestration)

Landed the **orchestration half** of Milestone 3, closing the agent loop end-to-end. Added the `AgentManager` multiplexer + process registry (global concurrency cap + LRU idle eviction) wired into `cmd_serve`; the agent→BE **MCP callback server** (`WorkspaceMcpServer`) over the same `Arc<dyn WorkspaceApi>` the FE uses, universal MCP-config translators (ACP/auggie/codex/opencode/claude), baseline-env injection + secret redaction, and the per-agent-type tool denylist (§18.4); and the full **`agent.*` RPC surface** (24 methods). Wired live MCP **spawn-wiring**: `AgentManager::create_agent` stands up a per-agent denylisted MCP server, serves it over a loopback **bridge** (`intent-acp::mcp_bridge` — a TCP NDJSON listener + the `intentd mcp-bridge` stdio↔TCP proxy), and writes the generated `--mcp-config` so a real spawned child reaches the in-process server. Authored the deterministic **mock ACP agent** (node, gated by `MOCK_AGENT_SCRIPT_PATH`) and a hermetic spawned-child **E2E** that drives a full turn whose work is a real `add_to_note` MCP `tools/call` (asserts BE state change + streamed chunks + exactly one `agent:stream:end` + persisted conversation — not an in-process shortcut). Extended `doctor` with provider discovery (installed/authed) + an explicit migration `0004` gate. A real auggie turn in CI stays deferred (best-effort/local). CI green on all three build targets (incl. Windows). Submodule HEAD `b0ca816`.

### Milestone 3 — M3.10 (agent runtime wiring)

Closed out Milestone 3 by wiring the `agent.*` RPC handlers to the `AgentManager` **live runtime** (previously stubbed) — Milestone 3 is now functionally complete end-to-end. `agent.sendMessage` lazily spawns the child on the first turn (TS spawn-on-first-message semantics), drives the ACP turn through `AgentManager::run_turn` in a background worker, and drains the in-memory send queue via the **queue-flip** loop (each queued message flips to in-flight when the current turn ends); `agent.forceMessage` aborts the in-flight worker (interrupt) then delivers immediately; `agent.stop` aborts the in-flight turn worker, clears the busy flag, drops the handle (killing the daemon-spawned child via `kill_on_drop`), and deregisters. Added the **over-the-wire E2E** `tests/uds_agent_runtime.rs` that launches the real `intentd serve` daemon (mock provider) and drives `agent.create` + `agent.sendMessage` over UDS — asserting the daemon-spawned child reached the per-agent workspace MCP server via the generated `--mcp-config` (a real `tools/call` mutating the note) and that both the `note:updated` domain event and the terminal `agent:stream:end` arrive over the transport. Touched `intent-services` (incl. `agent_ops`, `agent_manager`) and the `intentd` test suite. Two parity follow-ups deferred to Milestone 5: the keep-alive stop variant via `provider.interrupt` (stop currently kills the child rather than interrupting + keeping the session warm), and `agent.stop` emitting no terminal `agent:stream:end`. CI green on all three build targets (incl. Windows). Submodule HEAD `3e788f2`.

### Milestone 4 — Cycle A (local git)

Landed the **local git** half of Milestone 4 — a new `intent-git` crate (git2/libgit2; depends on `intent-core` only per §3.2) and the full six-method `git.*` JSON-RPC surface, routed end-to-end (`intent-git` → `intent-services` `git_ops` → `intent-transport`). Methods: `git.status` (file-level `GitStatus`), `git.stage` (CSV-string or array paths; staging all → `-32603`), `git.getBranches` (`repoPath`+`includeRemote?`; unknown path → `-32602`), `git.commit` (deprecated; empty-commit guarded), `git.agentCommit` (`{ ok, hash, files, fileCount }`), `git.checkMergeConflicts` (libgit2 `merge_base`+`merge_trees`; no base → `cannotDetermine`). Added two internal helpers for Cycle C (no wire method yet): a lazy diff helper (additions/deletions + hunks via blob SHAs) and a worktree-create + per-path async lock (`WorktreeLocks`). Parity-exact shapes/casing vs the TS `ws-git-api` ground truth; proven by a UDS write-ops round-trip test. Accepted deferrals: `agentCommit` file-tracking attribution → Cycle C; `autoCommit` gate → settings store; `Agent-Id` trailer; libgit2 hooks/merge-tree; `getBranches` known-repo set; worktree mutex on the commit path. Touched `intent-core`, `intent-git`, `intent-services`, `intent-transport`, `intentd`. CI green on all three build targets (incl. Windows). Submodule HEAD `869a3ab`.

### Milestone 4 — Cycle B (GitHub / PR)

Landed the **GitHub / PR** half of Milestone 4 — a new `intent-sourcecontrol` crate and the full 12-method `pr.*` JSON-RPC surface, plus PR↔workspace linking. The crate provides the `SourceControl` trait + `GitHubSourceControl` over **octocrab** (REST + GraphQL), token resolution (keyring → env → `gh` CLI), GitHub Enterprise (GHE) base-URL support, a provider registry, and graceful `NotConfigured` behavior. The `pr.*` surface is 5 reads + 7 writes/actions, all active-PR gated (`-32603` when no active PR is linked), routed end-to-end (`intent-sourcecontrol` → `intent-services` `pr_ops` → `intent-transport`). PR↔workspace linking matches by `head.ref` (branch-only), persists `prStatus` + `activePullRequest` via the `0005_workspace_pr` migration, and runs a 60s background + on-demand refresh emitting `pr:linked`/`pr:updated`/`pr:unlinked`. Accepted deferrals: branch-only matching (vs TS branch-OR-`baseRef`); `pr:*` wire events as an intentd extension; capability gating + pagination deferred; `pr.listComments` host-agnostic shape; RFC-3339 timestamps. Touched `intent-core`, `intent-sourcecontrol`, `intent-services`, `intent-store`, `intent-transport`, `intentd`. CI green on all three build targets (incl. Windows). Submodule HEAD `278f815`.

### Milestone 4 — Cycle D (Search) — Milestone 4 COMPLETE

Landed the **Search** surface — a new `intent-search` crate and the full eight-method `search.*` JSON-RPC surface — **completing Milestone 4** (local git + GitHub/PR + Code Changes Review + Search all landed). The crate provides gitignore-aware content + filename search over `ripgrep`/`ignore`/`globset` (honors `.gitignore`, a deliberate divergence from the TS behavior) and a `CancelRegistry` for in-flight cancellation. The `search.*` surface (`inFiles`, `fileNames`, `messages`, `events`, `memories`, `notes`, `codebase`, `cancel`) is routed end-to-end (`intent-search` → `intent-services` `search_ops` → `intent-transport`): `inFiles`/`fileNames` search the filesystem, `messages`/`events`/`notes` are store-backed, and each search mints + echoes a `requestId` (tracked via the `CancelRegistry`) and streams incremental `search:result` events plus a terminal `search:done` over the event bus. Accepted deferrals: `search.memories` store/RPC → Milestone 9 (surface present, returns empty); `search.codebase` context-engine integration → later (surface present). Touched `intent-core`, `intent-search`, `intent-services`, `intent-transport`, `intentd`. CI green on all three build targets (incl. Windows). Submodule HEAD `0856129`.

### Milestone 4 — Cycle C (Code Changes Review)

Landed the **Code Changes Review** surface — the BE-internal change-tracking + attribution pipeline, metrics aggregation, and accept-changes orchestration — closing out the wire side of Milestone 4. Added the additive `0006_code_review` migration (the `tracked_changes` per-file audit trail; raw content kept lazy via git blob SHAs, never inlined) plus the `tracked_changes_repo`, `diffs_repo`, and `metrics_repo` stores (`intent-store`). Added **17 JSON-RPC methods** routed end-to-end (`intent-services` → `intent-transport`): `file-tracking.*` (8) — `init`, `sync`, `load`, `getChanges`, `loadCommits`, `getLineStats`, `stage`, `unstage`; `metrics.*` (4) — `getWorkspaceStats`, `getAgentStats`, `getAllWorkspaceStats`, `clearAgentStats`; and `accept-changes.*` (5) — `getStatus`, `prepare`, `execute`, `mergePR`, `addRemote`. `accept-changes.execute` implements `commit` (cascading → `push` → `create-pr`), `push`, and `create-pr`, with `accept-changes.mergePR` performing the PR merge; the `ChangeStage` gains a `pull_request` stage (`pushed`→`pull_request`→`merged`). The `intent-git` `WorktreeLocks` mutex is now wired on the services commit path. Emits the new `changes:metrics-changed` event (alongside `changes:tracked`/`changes:git-status`). Deferred: the `accept-changes.execute` actions `merge`/`export`/`undo-push`/`undo-commit`/`reset-to-trunk`/`rebase-onto-trunk` (pending libgit2 merge/rebase/fetch + remote RPC; PR-merge available via `accept-changes.mergePR`). Touched `intent-core`, `intent-git`, `intent-services`, `intent-store`, `intent-transport`, `intentd`. CI green on all three build targets (incl. Windows). Submodule HEAD `f1e7329`.

### Milestone 5 — Cycle E (WSS transport)

Landed the **WSS transport** — a secure WebSocket listener alongside the existing UDS transport, wire-identical via a shared router/connection layer. Added `transport::tls` (rcgen self-signed EC P-256 cert generation + SHA-256 fingerprint pinning; `ring` as the sole rustls crypto provider), `transport::auth` (keychain-backed bearer token, timing-safe validation, token extraction from the `Authorization` header or `?token=`, and an origin allow-list), and `transport::ws` + `transport::lifecycle` (an HTTPS + WS listener on `0.0.0.0:5180` exposing `/ws` + `/health`, upgrade-time auth returning `401`/`403`, port backoff, single-flight start/stop, graceful shutdown with no `EADDRINUSE`, a 30s/60s heartbeat, and a post-bind handler). WSS ↔ UDS parity is guaranteed by the shared `transport::conn` router/connection code. Accepted deferrals: mDNS discovery → M5.4; single-instance enforcement + idle reaping → M5.5; daemonization / import / retention / full E2E → Cycles F/G. Touched `intent-transport` (`tls`, `auth`, `ws`, `lifecycle`, `conn`) and `intentd`. CI green on all three build targets (incl. Windows). Submodule HEAD `e6d7594`.

### Milestone 5 — Cycle F (CLI control + daemonization)

Landed the **CLI control + daemonization** half of Cycle F (M5.6) — the operator-facing daemon-control surface. Added a **UDS-only control fast-path** (`transport::control`, the `SystemControl` trait — `intent-transport`) handling `system.status`/`system.shutdown`, intercepted before the JSON-RPC dispatcher (`serve_uds` gains an optional control handler; `detect_has_display` + `inspect_cert` exposed). These `system.*` methods are **intentd-only** ops/control methods — not part of the ported 106 and not advertised by `getSupportedMethods`. `intentd status` now renders the live `system.status` RPC (liveness, transports, port, clients, agents, cert fingerprint, host caps) with a clean down fallback; `intentd stop` issues a graceful `system.shutdown` then escalates SIGTERM→SIGKILL via the pidfile (behind a testable `Signaller` seam); `intentd doctor` is hardened with ports-free, cert-validity, `gh`/`GITHUB_TOKEN`, context-engine, and `hasDisplay`/locality checks; and `intentd service install|uninstall|status` generates a launchd plist + systemd user unit (golden-string tests, platform-gated, no double-fork). Tests: control unit tests, stop-escalation unit tests, service golden tests, and a `uds_control` serve→stop→restart E2E (existing UDS/WSS tests updated only to pass `None` for the new `serve_uds` control arg). Deferred: import / retention / full E2E / security review → Cycle G. Touched `intent-transport`, `intentd`. CI build matrix green on all three targets (incl. Windows). Submodule HEAD `e7cab9b`.

### Milestone 5 — Cycle F (discovery + reaping)

Landed **mDNS service discovery** and **single-instance enforcement + idle agent reaping**, advancing Milestone 5. Discovery (`transport::discovery`, `intent-transport`) advertises the daemon as `_intent-ws._tcp` with `version`/`path`/`hostname`/`fp` plus `os`/`arch`/`hasDisplay`/`locality` TXT records, config-gated, advertised post-bind and unpublished on shutdown. Single-instance + reaping spans `intentd`/`intent-services`/`intent-acp`/`intent-core`: a **pidfile guard** with a **UDS liveness probe** + stale pidfile/socket cleanup (a second daemon refuses to start only when the first is actually serving), a **TTL idle-reap sweep** extending the LRU agent registry (new config knob `idleReapMinutes`, default 30), and **process-group tree-kill** (SIGTERM→SIGKILL via `nix`) backed by `process_group(0)` at spawn so a reaped/torn-down agent's whole child tree is removed (not just the direct child). The two landed via disjoint intentd PRs (discovery #12, single-instance + reaping #13); non-Unix builds stay green via `cfg`-gated UDS-liveness + process-group code. Deferred: M5.6 CLI control (`intentd stop` + status/doctor hardening) + launchd/systemd daemonization land later in Cycle F; import / retention / full E2E / security review → Cycle G. Touched `intent-transport`, `intentd`, `intent-services`, `intent-acp`, `intent-core`. CI green on all three build targets (incl. Windows). Submodule HEAD `48dbd22`.

### Milestone 9 — Agent ecosystem

Landed the **agent ecosystem** — five cohesive surfaces. **(1)** A **settings store** (the additive `0008_settings.sql` store + `settings_repo`) behind the `settings.*` surface (`list`/`get`/`update`/`reset`): typed get/list + an **atomic batch `update`** with `-32602` validation, **keychain-backed redaction** of sensitive keys (`workspace.sshKeyPath`, `mcp.servers`, `server.auth.token`, `sourceControl.github.token`), the self-sufficient `settings:changed` event, and the previously-deferred **`git.autoCommit` gate** now wired onto this store. **(2)** `rules.*` (`list`/`get`/`update`) over the `endUserRules` setting plus the internal **prompt-assembly/injection** pipeline (base → specialization → workspace override → live rule files) wired into agent creation, alongside the per-agent-type **tool denylist**. **(3)** `specialist.*` full CRUD (`list`/`get`/`create`/`edit`/`delete`) including the previously-missing ported **`specialist.list`** with **3-tier resolution** (project > user > bundled; bundled read-only; markdown-with-frontmatter storage). **(4)** `mcp.servers.*` (7: `list`/`create`/`update`/`delete`/`toggle`/`restart`/`getStatus`) over the sensitive `mcp.servers` setting (env/headers redacted), backed by an **`McpHub`** that spawns/stops/restarts external **stdio** MCP servers (handshake → toolCount) with a health monitor + auto-restart, the self-sufficient **`mcp.servers:status-changed`** event per transition, and process-group reaping on shutdown (http/sse transports deferred via an error status; the §6.8 agent→BE callback is the opposite direction and untouched). **(5)** the `memories` table (the additive `0009_memories.sql` migration + `idx_memories_ws` + `memories_repo`) backing **`search.memories`** — promoted from the M4 empty stub to a real store-backed substring search returning `MemoryMatch[]` per PROTOCOL §5.15 (**no `memories.*` RPC** — deferred §18.5). 583 tests green; landed via intentd PR #28. Touched `intent-core`, `intent-search`, `intent-services`, `intent-store`, `intent-transport`, `intentd`. CI green on all three build targets (incl. Windows). Submodule HEAD `0f1f2f8`.

## Next steps / open questions

- **Milestone 10 is COMPLETE** — the context-engine slice (`ContextEngine` trait + `AuggieContextEngine` + binary discovery; `search.codebase` ripgrep-backed with graceful degradation; `doctor` availability) plus protocol-completeness fixes (`Event.metadata`; `task:ready-tasks-changed` + distinct `file:*` types; `agent.stop` interrupt keep-alive + terminal `stream:end`; an env-gated real-auggie e2e). Three deferrals are now **closed as won't-port**: the `memories.*` RPC (vestigial in-memory stub), `mcp.servers` **http/sse** transports (stdio only), and the `accept-changes.execute` **`export`** action (no UI, absent from the new FE). **Option A** for context: auggie exposes no structured codebase-retrieval CLI, so `retrieve()` degrades instantly and ripgrep stays the backing for `search.codebase`.
- **Milestone 7 is COMPLETE** — stable client identity (`client.hello`) + BE-persisted per-client chat drafts (`drafts.*`: `get`/`set`/`clear`) + the `draft:changed` event, backed by the additive `0007_client_drafts.sql` migration (`client` + `draft` tables), landed via intentd PR #26.
- **Milestone 5 is COMPLETE** (Cycle G closed with the §11.3 security review + hardening). **Milestone 6 (Iteration 3) is COMPLETE** — the `intent-pty` host (T1), the `terminal.*` RPC + ACP `terminal/*` wire surface (T2), `script.*` reconciled onto the same PTY host (T3), the workspace activity/attention status model (T4), locality detection + the `host.status` capability probe (T5), and the remote-only mitigations (`forward.*` loopback tunnels + the FE-served `host.openExternal` reverse RPC + headless warning) (T6) have all landed.
- Continue expanding the JSON-RPC method catalog beyond the core CRUD + event surface, driven by PROTOCOL.md.
- Harden transport panic-safety (`catch_unwind` → `-32603`) before the full method catalog.

## Changelog

Append a dated entry (newest first) whenever a meaningful unit of porting work lands. Keep each entry concise: what changed, which crates/methods, and the resulting submodule HEAD.

- **2026-06-24** — Milestone 10 — context engine + protocol-completeness + won't-port doc closures: **(CE-1)** a `ContextEngine` trait + `AuggieContextEngine` + binary discovery (enhanced-PATH `auggie` resolution); **(CE-2)** `search.codebase` wired to the context engine with a **ripgrep fallback** + a `doctor` availability check; **(PC-1)** the `Event.metadata` field with a store round-trip; **(PC-2)** `task:ready-tasks-changed` emission + distinct `file:*` event types; **(PC-3)** `agent.stop` interrupt **keep-alive** (via `provider.interrupt`, session stays warm) + a terminal `stream:end`; **(PC-4)** an env-gated (`INTENTD_AUGGIE_E2E`) real-auggie context-engine integration test; **(CE-3, Option A)** `AuggieContextEngine::retrieve()` now degrades **instantly** (no spawn) — verified that auggie 0.29.0 exposes no structured codebase-retrieval CLI and the original `codebase:search` was a never-implemented stub with no caller, so `search.codebase` stays ripgrep-backed; **(DOC-1)** these doc closures — `memories.*` (vestigial stub), `mcp.servers` **http/sse** (stdio only), and `accept-changes.execute` **`export`** (no UI / absent from the new FE) recorded as **NOT PORTING (won't port)**, plus the Option-A context-engine reality. Touched `intent-context`, `intent-search`, `intent-services`, `intent-core`, `intent-acp`, `intentd` (with docs in the monorepo). Submodule HEAD `(set on submodule-bump PR)`.

- **2026-06-23** — Milestone 9 — agent ecosystem (settings, rules, specialists, mcp.servers, memories): **(1)** a **settings store** (`0008_settings.sql` + `settings_repo`) behind `settings.*` (`list`/`get`/`update`/`reset`) — typed get/list + atomic batch `update` (`-32602` validation), **keychain-backed redaction** of sensitive keys (`workspace.sshKeyPath`, `mcp.servers`, `server.auth.token`, `sourceControl.github.token`), the `settings:changed` event, and the previously-deferred **`git.autoCommit` gate** now wired onto it. **(2)** `rules.*` (`list`/`get`/`update`) over the `endUserRules` setting + the internal **prompt-assembly/injection** pipeline (base → specialization → workspace override → live rule files) wired into agent creation, plus the per-agent-type **tool denylist**. **(3)** `specialist.*` full CRUD (`list`/`get`/`create`/`edit`/`delete`) incl. the ported **`specialist.list`** with **3-tier resolution** (project > user > bundled; bundled read-only; markdown-with-frontmatter). **(4)** `mcp.servers.*` (7: `list`/`create`/`update`/`delete`/`toggle`/`restart`/`getStatus`) over the sensitive `mcp.servers` setting (env/headers redacted) backed by an **`McpHub`** spawning/stopping/restarting external **stdio** MCP servers (handshake → toolCount) with a health monitor + auto-restart, the self-sufficient **`mcp.servers:status-changed`** event, and process-group reaping on shutdown (http/sse deferred via an error status; the §6.8 agent→BE callback is the opposite direction and untouched). **(5)** the `memories` table (`0009_memories.sql` + `idx_memories_ws` + `memories_repo`) backing **`search.memories`** — now a real store-backed substring search returning `MemoryMatch[]` per §5.15 (**no `memories.*` RPC** — deferred §18.5). 583 tests green. Landed via intentd PR #28. Touched `intent-core`, `intent-search`, `intent-services`, `intent-store`, `intent-transport`, `intentd`. Submodule HEAD `0f1f2f8`.
- **2026-06-22** — Milestone 8 — deferred `accept-changes.execute` git actions via libgit2: the five previously-deferred `accept-changes.execute` actions — `undo-commit`, `undo-push`, `reset-to-trunk`, `rebase-onto-trunk`, `merge` — are now implemented end-to-end on **libgit2 primitives (no shelling out to `git`)**, routed `intent-services` → `intent-transport`. **(T1)** new `intent-git` primitives: `auth` (remote credential callback), `fetch`, `reset`, `refs`, `ls_remote`, `squash`, `stash`, `rebase_with_autostash`, and `push_refspec`. **(T2)** the five execute actions wired on top of those primitives. Wire-for-wire parity-checked against the TS `accept-changes.service.ts`; **`export` remains deferred** (documented `exportPath` / path-safety reason). 569 tests green. Landed via intentd PR #27. Touched `intent-git`, `intent-services`, plus a new `intentd` integration test (`tests/uds_accept_changes.rs`). Submodule HEAD `be29a53`.
- **2026-06-21** — Milestone 7 — stable client identity (`client.hello`) + BE-persisted per-client drafts (`drafts.*`) — **Milestone 7 COMPLETE**: **(a)** **`client.hello`** (an intentd transport extension) registers a **logical, client-supplied `clientId`** that survives reconnects — the BE upserts the `client` row (human `name` + JSON capability bag), **mints a `clientId` server-side when the client omits one**, is **idempotent on re-hello**, and returns a server **capability block**; the ephemeral per-connection id (`ws-<ts>-<rand>`) stays transport-only and is never persisted. **(b)** the **`drafts.*`** namespace (`drafts.get`/`drafts.set`/`drafts.clear`, intentd transport extensions) — BE-persisted per-client chat drafts that replace the FE's per-client `localStorage` `chatDrafts`, keyed by `(workspaceId, agentId, clientId)` where the `clientId` is resolved **connection-side** from `client.hello` (minted on a mutation if hello was skipped) and is **never a wire parameter**; an empty `drafts.set` text is a **clear** (row deleted; returns `Some(updatedAt)` when stored, `None` when cleared), `drafts.clear` is **idempotent**, and clients are strictly **isolated** from one another. **(c)** the self-sufficient **`draft:changed`** event (`{ workspaceId, agentId, clientId, hasDraft }`) emitted by `set`/`clear`, deliberately **omitting the draft text** (no leakage) — it only signals that a client's draft exists or was cleared. Backed by the **additive** `0007_client_drafts.sql` migration (the `client` + `draft` tables; `0001`–`0006` untouched). Landed via intentd PR #26. Touched `intent-core`, `intent-store`, `intent-services`, `intent-transport`. Submodule HEAD `b54336d`.
- **2026-06-21** — Milestone 6 (Iteration 3) — locality detection + host capabilities & remote-only mitigations — **Milestone 6 COMPLETE**: **(a)** **locality detection & host capabilities** — a `host.status` probe returning `{ os, arch, hostname, hasDisplay, locality, displayServer? }` answered on **both** UDS and TCP/WSS, backed by a pure `resolve_is_local()` (UDS / `--mode local` ⇒ local, TCP/WSS ⇒ remote, with a `server.locality` setting override winning), and `hasDisplay`/`locality` folded into the `status`/`doctor` reports and the mDNS TXT record; **(b)** **remote-only mitigations** — `forward.*` (`create`/`list`/`close`) real per-connection **loopback TCP tunnels** (no-op when local) with a registry + connection-drop cleanup, an FE-served **`host.openExternal`** as a daemon→client **reverse RPC** (a `ReverseChannel` mirroring the ACP client-served pattern — resolves locally via an injectable opener when local+`hasDisplay`, proxies to the connected client when remote), and a clear **headless warning** when local+`hasDisplay`=false. With this entry **Milestone 6** (the unified PTY host for terminals + scripts, the workspace activity/attention status model, locality/capabilities, and remote mitigations) is **complete**. Landed via intentd PRs #24 (locality + `host.status`) + #25 (`forward.*` + `host.openExternal` + headless warning). Touched `intent-transport`, `intentd`. Submodule HEAD `0a56733`.
- **2026-06-21** — Milestone 6 (Iteration 3) — `terminal.*` + `script.*` on the unified PTY host: **(a)** the interactive **`terminal.*`** JSON-RPC surface (`create`/`write`/`resize`/`kill`/`getBuffer`/`list`) over the `intent-pty` host (`intent-core` `WorkspaceApi` → `intent-services` `terminal_ops` → `intent-transport`) — base64-framed `terminal:data` (`chunk`) + a terminal `terminal:exit` (`{ terminalId, exitCode, signal }`); a late attach **back-fills** scrollback via `terminal.getBuffer` then tails live, multi-subscriber output is identical, and the streamer polls the child so a **natural exit** still emits `terminal:exit`. The M3 ACP **`terminal/*`** stub is **replaced** with a real host-backed implementation (a PTY-free `TerminalHost` trait in `intent-acp`, implemented by `intent-services` `PtyTerminalHost` and wired into the `ClientRequestHandler`; the `-32601` stub remains only as the no-host fallback). **(b)** the full nine-method **`script.*`** surface (`list`/`create`/`remove`/`start`/`stop`/`restart`/`run`/`output`/`status`) reconciled onto the **same** unified PTY host (no separate spawn path), via `intent-services` `script_ops` → `intent-transport`: service-mode auto-restart with backoff, command-mode `script.run` with timeout, dev-server URL/port detection (`detectedUrl` surfaced on `script:state`), live `script:output` (base64 `chunk`) streaming, `script.output` returning **plaintext** last-N-lines buffer text per §5.8 parity, and a workspace-scoped script PTY attachable via `terminal.*`. Landed via intentd PRs #22 (`terminal.*`) + #23 (`script.*`). Touched `intent-core`, `intent-pty`, `intent-acp`, `intent-services`, `intent-transport`. Submodule HEAD `1286eb1`.
- **2026-06-20** — Milestone 6 (Iteration 3) — PTY host + workspace status model: **(a)** a new `intent-pty` crate (`intent-core`-only per §3.2) — a unified `portable-pty` **host** (`PtyHost`) where interactive terminals **and** scripts run as real PTYs in one host (so a terminal can attach to a running script), with bounded server-side scrollback (`scrollback::Scrollback`, porting `script-output-buffer.ts`; **back-fill recent history then tail live** so output survives client disconnects), multi-client output fan-out (broadcast, porting `MainProcessTerminalManager.ts`) with **serialized** stdin into the single PTY master, resize + SIGINT signal delivery, and **session/workspace-scoped** process-group reaping (the PTY child is a `setsid` leader so `killpg` reaps the whole tree — no orphans). Engine-only: exposes `PtyHost`/`SpawnSpec`/`Attachment`/`PtyId`/`PtySize`/`PtySignal` for the wire layers (`terminal.*`/`script.*`, deferred to later in Milestone 6) and contains no wire methods/script policy/status model. **(b)** the **workspace activity/attention status model** (§9.9) — a derived `WorkspaceActivity` (`Idle`/`AgentRunning`, **never persisted**, computed from live agent state and filled on `workspace.list`/`get`; `AgentManager` tracks a per-workspace running-agent count on start/teardown), the server-owned persisted `attention` (`None`/`Unread`/`ReviewRequired`, **reusing the existing column** + the existing `workspace.markSeen`/`dismissAttention` RPCs, plus a BE `raise_attention`), and self-sufficient **only-on-change** `workspace:activity-changed { workspaceId, activity }` + `workspace:attention-changed { workspaceId, attention }` events (§10.1). Landed via intentd PRs #20 (PTY host) + #21 (status model). Touched `intent-pty`, `intent-services`, `intent-core`. Submodule HEAD `90ba033`.
- **2026-06-20** — Milestone 5 — Cycle G (§11.3 security review + hardening) — **Cycle G + Milestone 5 COMPLETE**: a focused audit of the §11.3 requirements found **all 8 items already implemented in production with no gaps** — UDS socket `0600`, timing-safe bearer-token comparison, origin allow-list, TLS fingerprint-pinning / TOFU, path sandboxing, process-group reaping, headless default-deny posture, and secret hygiene. Work is **test-only** regression coverage locking in those guarantees: a new `intentd/tests/security_hardening.rs` suite + extended `intent-acp` tests (+178/-4). Gate green (test/fmt/clippy). Landed via intentd PR #19. Touched `intent-acp`, `intentd` (tests only). Submodule HEAD `aa2ee12`.
- **2026-06-20** — Milestone 5 — Cycle G (ACP integration harness + E2E transport/lifecycle): a **hermetic mock ACP provider** (§13.2, `intent-acp`) replaying scripted NDJSON frames over the real `Connection`/handshake/session lifecycle + client-served `ClientRequestHandler` — happy-path turn, auth-required, mid-turn cancel, permission deny/allow, oversized-stderr ring-buffer truncation + terminal stub (`-32601`), malformed-frame recovery, stdin write-serialization (fixtures `tests/fixtures/*.ndjson`, directory-override `MOCK_AGENT_SCRIPT_PATH`); plus the **E2E transport + lifecycle suite** (§13.1, `intentd/tests/e2e_transport.rs`) booting a real `intentd serve` over both UDS + TCP/TLS — `e2e_transport_full` asserts live `system.status` transports `["uds","tcp"]`, WSS bearer-auth `401`/accept, origin allow-list `403`/`101`, the pinned TLS fingerprint matching the on-disk cert + mDNS-advertised `fp`, an mDNS advertise→resolve round-trip, byte-identical WSS vs UDS results, `intentd status`/`doctor`, and graceful `stop` + same-port restart with no `EADDRINUSE`; `e2e_idle_session_reaping` drives one mock-ACP turn and asserts the idle-TTL sweep evicts the agent. Hermetic per-daemon data dir + OS-assigned port via env-gated test-only seams (`INTENTD_AUTH_TOKEN`/`INTENTD_TCP_PORT`/`INTENTD_DISCOVERY`/`INTENTD_IDLE_REAP_MS`, production defaults preserved). Includes a deterministic `tests::pr` connection-pool-timeout de-flake — pause the tokio clock only after the first SQLite read completes (a `Notify` barrier on the stub forge's first `get_pr`), so the pool `acquire_timeout` can no longer fire spuriously; no assertions weakened. Landed via intentd PRs #17 (mock ACP) + #18 (E2E). Touched `intent-acp`, `intent-services`, `intentd`. Submodule HEAD `0f353b3`.
- **2026-06-20** — Milestone 5 — Cycle G (data migration + retention): `intentd import --from <legacy-store>` — an **idempotent, read-only** §9.7 data migration replaying a legacy store through the existing `intent-store` repos (no new wire methods, no schema changes; `intentd`); plus a §10.2 **event retention/compaction sweep** trimming old `agent:stream:*` chunk events while preserving every other family — `Store::delete_stream_events_before(cutoff)` (sole delete path, `agent:stream:` prefix + strict `< cutoff`; `intent-store`), `events::AGENT_STREAM_PREFIX` + `Config.stream_retention_hours` from `events.streamRetentionHours` (camel/snake; env `INTENTD_STREAM_RETENTION_HOURS`; default `0` = disabled; `intent-core`), and `spawn_stream_retention_loop` in the periodic-task loop beside the idle-reap sweep (disabled at TTL=0, aborted on shutdown; `intentd`). No VACUUM (WAL checkpointing reclaims space). Tests: preserve-other-families sweep, idempotent re-run, disabled/over-old no-op, config parsing. Landed via disjoint intentd PRs (import #15, retention #16). Deferred: §11.3 security review + hardening. Touched `intent-core`, `intent-store`, `intentd`. Submodule HEAD `db21275`.
- **2026-06-20** — Milestone 5 — Cycle F (CLI control + daemonization): operator-facing daemon control closing M5.6. A **UDS-only control fast-path** (`transport::control`, the `SystemControl` trait — `intent-transport`) handling `system.status`/`system.shutdown`, intercepted before the JSON-RPC dispatcher (`serve_uds` gains an optional control handler); these are intentd-only ops methods, **not** part of the ported 106 / `getSupportedMethods`. `intentd status` renders live `system.status` (liveness, transports, port, clients, agents, cert fingerprint, host caps) with a clean down fallback; `intentd stop` issues a graceful `system.shutdown` then escalates SIGTERM→SIGKILL via pidfile (testable `Signaller` seam); `intentd doctor` gains ports-free / cert-validity / `gh`+`GITHUB_TOKEN` / context-engine / `hasDisplay`+locality checks; `intentd service install|uninstall|status` generates a launchd plist + systemd user unit (golden-string tests, platform-gated, no double-fork). Tests: control + stop-escalation + service-golden units and a `uds_control` serve→stop→restart E2E. Deferred: import / retention / full E2E / security review → Cycle G. Touched `intent-transport`, `intentd`. Submodule HEAD `e7cab9b`.
- **2026-06-20** — Milestone 5 — Cycle F (discovery + reaping): **mDNS service discovery** (`transport::discovery`, `intent-transport`) advertising `_intent-ws._tcp` with `version`/`path`/`hostname`/`fp` + `os`/`arch`/`hasDisplay`/`locality` TXT, config-gated, advertised post-bind and unpublished on shutdown; plus **single-instance enforcement + idle agent reaping** — a pidfile guard + UDS liveness probe + stale pidfile/socket cleanup (`intentd`), a TTL idle-reap sweep extending the LRU agent registry (config `idleReapMinutes`, default 30) (`intent-services`), and process-group tree-kill (SIGTERM→SIGKILL via `nix`) backed by `process_group(0)` at spawn (`intent-acp`/`intent-services`). Landed via disjoint intentd PRs (discovery #12, single-instance + reaping #13); non-Unix builds kept green via `cfg`-gated UDS-liveness + process-group code. Deferred: M5.6 CLI control (`intentd stop` + status/doctor hardening) + launchd/systemd daemonization → later in Cycle F; import / retention / full E2E / security review → Cycle G. Touched `intent-transport`, `intentd`, `intent-services`, `intent-acp`, `intent-core`. Submodule HEAD `48dbd22`.
- **2026-06-20** — Milestone 5 — Cycle E (WSS transport): a secure WebSocket listener alongside UDS, wire-identical via a shared router/connection layer (`transport::conn`). Added `transport::tls` (rcgen self-signed EC P-256 cert + SHA-256 fingerprint pinning; `ring` as the sole rustls provider), `transport::auth` (keychain bearer token, timing-safe validate, extract from `Authorization` header or `?token=`, origin allow-list), and `transport::ws` + `transport::lifecycle` (HTTPS + WS on `0.0.0.0:5180` exposing `/ws` + `/health`; upgrade auth `401`/`403`; port backoff; single-flight start/stop; graceful shutdown no `EADDRINUSE`; 30s/60s heartbeat; post-bind handler). Deferred: mDNS → M5.4; single-instance + idle reaping → M5.5; daemonization / import / retention / full E2E → Cycles F/G. Touched `intent-transport`, `intentd`. Submodule HEAD `e6d7594`.
- **2026-06-19** — Milestone 4 — Cycle D (Search) — **Milestone 4 COMPLETE**: new `intent-search` crate (gitignore-aware content + filename search over `ripgrep`/`ignore`/`globset` — honors `.gitignore`, a deliberate divergence from TS; `CancelRegistry` for in-flight cancellation) + the full eight-method `search.*` surface routed end-to-end (`intent-search` → `intent-services` `search_ops` → `intent-transport`): `inFiles`/`fileNames` (filesystem), store-backed `messages`/`events`/`notes`, `memories` + `codebase` (surface present; backends deferred), and `cancel`. Each search mints + echoes a `requestId` (tracked via `CancelRegistry`) and streams incremental `search:result` + terminal `search:done` over the event bus. Deferred: `search.memories` store/RPC → M9; `search.codebase` context-engine → later. Touched `intent-core`, `intent-search`, `intent-services`, `intent-transport`, `intentd`. Submodule HEAD `0856129`.
- **2026-06-19** — Milestone 4 — Cycle C (Code Changes Review): the BE-internal change-tracking + attribution pipeline, metrics aggregation, and accept-changes orchestration. Added the additive `0006_code_review` migration (`tracked_changes` audit trail; lazy blob-SHA content) + `tracked_changes_repo`/`diffs_repo`/`metrics_repo` (`intent-store`); and **17 JSON-RPC methods** routed end-to-end — `file-tracking.*` (8: `init`, `sync`, `load`, `getChanges`, `loadCommits`, `getLineStats`, `stage`, `unstage`), `metrics.*` (4: `getWorkspaceStats`, `getAgentStats`, `getAllWorkspaceStats`, `clearAgentStats`), `accept-changes.*` (5: `getStatus`, `prepare`, `execute`, `mergePR`, `addRemote`). `execute` does `commit` (cascade → `push` → `create-pr`), `push`, `create-pr`; `mergePR` merges; `ChangeStage` gains `pull_request` (`pushed`→`pull_request`→`merged`); the `WorktreeLocks` mutex now wired on the commit path; emits `changes:metrics-changed`. Deferred: `merge`/`export`/`undo-push`/`undo-commit`/`reset-to-trunk`/`rebase-onto-trunk` (pending libgit2 merge/rebase/fetch + remote RPC). Touched `intent-core`, `intent-git`, `intent-services`, `intent-store`, `intent-transport`, `intentd`. Submodule HEAD `f1e7329`.
- **2026-06-19** — Milestone 4 — Cycle B (GitHub / PR): new `intent-sourcecontrol` crate (`SourceControl` trait + `GitHubSourceControl` over octocrab REST+GraphQL; token resolution keyring → env → `gh` CLI; GHE base-URL; provider registry; graceful `NotConfigured`) + the full 12-method `pr.*` surface (5 reads + 7 writes/actions, active-PR gated `-32603`) routed end-to-end (`intent-sourcecontrol` → `intent-services` `pr_ops` → `intent-transport`); plus PR↔workspace linking — `head.ref` branch-only matching, the `0005_workspace_pr` migration persisting `prStatus` + `activePullRequest`, and a 60s background + on-demand refresh emitting `pr:linked`/`pr:updated`/`pr:unlinked`. Deferred: branch-only matching (vs TS branch-OR-`baseRef`); `pr:*` wire events as an intentd extension; capability gating + pagination; `pr.listComments` host-agnostic shape; RFC-3339 timestamps. Touched `intent-core`, `intent-sourcecontrol`, `intent-services`, `intent-store`, `intent-transport`, `intentd`. Submodule HEAD `278f815`.
- **2026-06-19** — Milestone 4 — Cycle A (local git): new `intent-git` crate (git2/libgit2; depends on `intent-core` only) + the full six-method `git.*` surface routed end-to-end — `git.status` (file-level `GitStatus`), `git.stage` (CSV-string/array paths; staging all → `-32603`), `git.getBranches` (`repoPath`+`includeRemote?`; unknown path → `-32602`), `git.commit` (deprecated; empty-commit guarded), `git.agentCommit` (`{ ok, hash, files, fileCount }`), `git.checkMergeConflicts` (libgit2 `merge_base`+`merge_trees`; no base → `cannotDetermine`). Two internal Cycle-C helpers (no wire method yet): a lazy diff helper (blob-SHA hunks) and a worktree-create + per-path async lock (`WorktreeLocks`). Parity-exact vs TS `ws-git-api`; UDS write-ops round-trip test. Deferred: `agentCommit` attribution → Cycle C; `autoCommit` gate → settings store; `Agent-Id` trailer; libgit2 hooks/merge-tree; `getBranches` known-repo set; worktree mutex on commit path. Touched `intent-core`, `intent-git`, `intent-services`, `intent-transport`, `intentd`. Submodule HEAD `869a3ab`.
- **2026-06-19** — Milestone 3 — M3.10 (agent runtime wiring): wired the `agent.*` RPC handlers to the `AgentManager` live runtime (no longer stubbed) — `agent.sendMessage` lazy-spawns the child on the first turn, drives the ACP turn via `AgentManager::run_turn` in a background worker, and drains the send queue (queue-flip loop); `agent.forceMessage` interrupt (abort in-flight worker) → deliver; `agent.stop` cancel (abort worker + `kill_on_drop` child + deregister). Added the over-the-wire E2E `tests/uds_agent_runtime.rs` (real `intentd serve` daemon over UDS; daemon-spawned child performs a real MCP `tools/call` via the generated `--mcp-config`; asserts `note:updated` + terminal `agent:stream:end` over the transport). Milestone 3 functionally complete end-to-end. Deferred to M5: keep-alive stop via `provider.interrupt`; `agent.stop` emits no terminal `agent:stream:end`. Touched `intent-services` (`agent_ops`, `agent_manager`), `intentd` tests. Submodule HEAD `3e788f2`.
- **2026-06-19** — Milestone 3 — Cycle B (ACP orchestration): `AgentManager` + process registry (concurrency cap + LRU eviction) in `cmd_serve`; agent→BE MCP callback server (`WorkspaceMcpServer`) over the FE's `WorkspaceApi`, universal MCP-config translators + baseline-env/redaction + §18.4 tool denylist; 24 `agent.*` RPC methods; live MCP spawn-wiring — `create_agent` writes the generated `--mcp-config` and serves a per-agent denylisted MCP server over the `intent-acp::mcp_bridge` (TCP NDJSON listener + `intentd mcp-bridge` stdio↔TCP proxy); the node mock agent (gated by `MOCK_AGENT_SCRIPT_PATH`) + a hermetic spawned-child E2E doing a real `add_to_note` MCP `tools/call`; `doctor` provider discovery + migration `0004` gate. Deferred: real auggie turn in CI (best-effort/local). Touched `intent-core`, `intent-acp`, `intent-providers`, `intent-services`, `intentd`. Submodule HEAD `b0ca816`.
- **2026-06-19** — Milestone 3 — Cycle A (ACP foundation): new crates `intent-providers` (provider registry, model resolution) and `intent-acp` (client core — spawn, hand-rolled NDJSON transport, `initialize` handshake; session lifecycle `new`/`load`/`prompt`/`cancel` with `session/update`→event-bus streaming; client-served fs + permission-request flow + terminal stub); `AgentSession` entity + `agent_repo` persistence via the `0004_agent_session` migration; `WorkspaceApi` trait (`intent-core`). Deviations: hand-rolled NDJSON transport (acp-tokio 0.11/0.14 incompat), intentd-new `agent:permission:*` events (PROTOCOL §8), `name_explicitly_set` column. Foundation half; orchestration (AgentManager, MCP callback, `agent.*` RPC, E2E) is the upcoming Cycle B. Touched `intent-core`, `intent-store`, `intent-services`, `intent-providers`, `intent-acp`. Submodule HEAD `f5d2f8f`.
- **2026-06-18** — Milestone 2 — Events: append-only `Event` domain + `0003_events` log migration/repo; in-process `EventBus` + filter engine; bidirectional UDS transport (server-initiated `events.event` + `events.subscribe`/`unsubscribe` fast-path); seven `event.*` methods (incl. deprecated singular aliases); notify-based file watcher emitting `file:changed`; M1 CRUD mutations now emit change events. Deferred parity-safe: `task:ready-tasks-changed`, saga-driven `workspace:*`, `Event.metadata`; distinct `file:*` types → Milestone 8. Touched `intent-core`, `intent-store`, `intent-services`, `intent-transport`, `intentd`. Submodule HEAD `3238a73`.
- **2026-06-18** — Milestone 1 — core domain CRUD: 34 JSON-RPC methods (`workspace.*` 9, `note.*` 12, `task.*` 8, `comment.*` 5) over UDS; `0002_comments` migration; e2e UDS lifecycle test + camelCase parity fixtures; `doctor` migration check; Windows `cfg`-gating. Touched `intent-core`, `intent-services`, `intent-store`, `intent-transport`, `intentd`. Submodule HEAD `c989aeb`.
- **2026-06-17** — Breadcrumbs, docs index & `make dev`: added breadcrumbs trail, `docs/README.md` index, and AGENTS.md breadcrumb-update policy. Docs-only (monorepo); submodule HEAD unchanged @ `8e13a25`.
- **2026-06-17** — READMEs: wrote README.md for both repos. Submodule HEAD `8e13a25`.
- **UDS JSON-RPC slice** — services + transport + CLI + integration test. Submodule HEAD `2756eb4`.
- **Core + SQLite store** — `intent-core` + `intent-store` (domain model + SQLite). Submodule HEAD `41fd4a6`.
- **Crate skeleton** — Full 12-crate skeleton with stubs; dependency direction enforced.
- **Repo & CI bootstrap** — repos, scaffold, submodule, CI, cliff.toml.
