# Local dev data, including intentd state and `.dev/sandbox/*.json` identities
.dev/

# Local scratch dir for worktree commit-message drafts (never committed)
.wt-commit-details/

# macOS Finder metadata
.DS_Store

# Root-level scratch files
/.apply_*.py
/*.tmp
